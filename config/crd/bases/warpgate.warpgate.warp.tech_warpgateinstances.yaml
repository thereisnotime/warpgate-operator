---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: warpgateinstances.warpgate.warpgate.warp.tech
spec:
  group: warpgate.warpgate.warp.tech
  names:
    kind: WarpgateInstance
    listKind: WarpgateInstanceList
    plural: warpgateinstances
    singular: warpgateinstance
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.version
      name: Version
      type: string
    - jsonPath: .spec.replicas
      name: Replicas
      type: integer
    - jsonPath: .status.readyReplicas
      name: Ready
      type: integer
    - jsonPath: .status.endpoint
      name: Endpoint
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: WarpgateInstance is the Schema for the warpgateinstances API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of WarpgateInstance
            properties:
              adminPasswordSecretRef:
                description: adminPasswordSecretRef references a Secret containing
                  the initial admin password.
                properties:
                  key:
                    description: |-
                      key is the key within the Secret. Defaults to "password" for password refs.
                      Unused by tokenSecretRef (which always reads "username" and "password" keys).
                    type: string
                  name:
                    description: name is the name of the Secret.
                    type: string
                required:
                - name
                type: object
              configOverride:
                description: |-
                  configOverride allows providing a complete warpgate.yaml config that replaces
                  the auto-generated one. Mounted as a ConfigMap.
                type: string
              createConnection:
                default: true
                description: |-
                  createConnection when true (default) auto-creates a WarpgateConnection CR
                  pointing to this instance, so other CRDs can reference it.
                type: boolean
              databaseURL:
                description: |-
                  databaseURL overrides the default SQLite database with a PostgreSQL connection string.
                  Example: "postgres://user:pass@host:5432/warpgate"
                type: string
              externalHost:
                description: externalHost sets the external hostname for Warpgate
                  cookie domain and URL generation.
                type: string
              http:
                description: http configures the HTTP/HTTPS protocol listener.
                properties:
                  enabled:
                    default: true
                    description: enabled enables the HTTP listener. Defaults to true.
                    type: boolean
                  port:
                    default: 8888
                    description: port is the container port. Defaults to 8888.
                    format: int32
                    type: integer
                  serviceType:
                    default: ClusterIP
                    description: serviceType is the Kubernetes Service type (ClusterIP,
                      NodePort, LoadBalancer). Defaults to ClusterIP.
                    enum:
                    - ClusterIP
                    - NodePort
                    - LoadBalancer
                    type: string
                type: object
              image:
                description: image overrides the full image reference. Defaults to
                  ghcr.io/warp-tech/warpgate:<version>.
                type: string
              ingress:
                description: ingress configures an Ingress resource for the HTTP listener.
                properties:
                  annotations:
                    additionalProperties:
                      type: string
                    type: object
                  className:
                    type: string
                  enabled:
                    type: boolean
                  hosts:
                    items:
                      description: IngressHost defines a host entry for an Ingress
                        resource.
                      properties:
                        host:
                          type: string
                        paths:
                          items:
                            description: IngressPath defines a path entry for an Ingress
                              host.
                            properties:
                              path:
                                type: string
                              pathType:
                                type: string
                            required:
                            - path
                            type: object
                          type: array
                      required:
                      - host
                      type: object
                    type: array
                  tls:
                    items:
                      description: IngressTLS defines a TLS entry for an Ingress resource.
                      properties:
                        hosts:
                          items:
                            type: string
                          type: array
                        secretName:
                          type: string
                      type: object
                    type: array
                type: object
              kubernetes:
                description: kubernetes configures the Kubernetes protocol proxy listener.
                properties:
                  enabled:
                    description: enabled enables this protocol listener.
                    type: boolean
                  port:
                    description: port is the container port.
                    format: int32
                    type: integer
                type: object
              mysql:
                description: mysql configures the MySQL protocol proxy listener.
                properties:
                  enabled:
                    description: enabled enables this protocol listener.
                    type: boolean
                  port:
                    description: port is the container port.
                    format: int32
                    type: integer
                type: object
              nodeSelector:
                additionalProperties:
                  type: string
                description: nodeSelector constrains which nodes the pod can be scheduled
                  on.
                type: object
              postgresql:
                description: postgresql configures the PostgreSQL protocol proxy listener.
                properties:
                  enabled:
                    description: enabled enables this protocol listener.
                    type: boolean
                  port:
                    description: port is the container port.
                    format: int32
                    type: integer
                type: object
              recordSessions:
                description: recordSessions enables session recording. Defaults to
                  false.
                type: boolean
              replicas:
                default: 1
                description: replicas is the number of Warpgate pods. Defaults to
                  1.
                format: int32
                minimum: 1
                type: integer
              resources:
                description: resources defines CPU/memory requests and limits for
                  the Warpgate container.
                properties:
                  claims:
                    description: |-
                      Claims lists the names of resources, defined in spec.resourceClaims,
                      that are used by this container.

                      This field depends on the
                      DynamicResourceAllocation feature gate.

                      This field is immutable. It can only be set for containers.
                    items:
                      description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                      properties:
                        name:
                          description: |-
                            Name must match the name of one entry in pod.spec.resourceClaims of
                            the Pod where this field is used. It makes that resource available
                            inside a container.
                          type: string
                        request:
                          description: |-
                            Request is the name chosen for a request in the referenced claim.
                            If empty, everything from the claim is made available, otherwise
                            only the result of this request.
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  limits:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Limits describes the maximum amount of compute resources allowed.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                  requests:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Requests describes the minimum amount of compute resources required.
                      If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                      otherwise to an implementation-defined value. Requests cannot exceed Limits.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                type: object
              ssh:
                description: ssh configures the SSH protocol listener.
                properties:
                  enabled:
                    description: enabled enables the SSH listener.
                    type: boolean
                  port:
                    default: 2222
                    description: port is the container port. Defaults to 2222.
                    format: int32
                    type: integer
                  serviceType:
                    default: ClusterIP
                    description: serviceType for the SSH service. Defaults to ClusterIP.
                    enum:
                    - ClusterIP
                    - NodePort
                    - LoadBalancer
                    type: string
                type: object
              sshKeysSecretName:
                description: |-
                  sshKeysSecretName references a Secret containing SSH host/client keys.
                  Expected keys: client-ed25519, client-rsa, host-ed25519, host-rsa.
                  If not set, Warpgate generates its own keys on first boot.
                type: string
              storage:
                description: storage configures persistent storage for Warpgate data.
                properties:
                  enabled:
                    default: true
                    description: enabled enables persistent storage via PVC. When
                      false, uses emptyDir.
                    type: boolean
                  existingClaimName:
                    description: existingClaimName uses an existing PVC instead of
                      creating one.
                    type: string
                  size:
                    default: 1Gi
                    description: size is the PVC size. Defaults to 1Gi.
                    type: string
                  storageClassName:
                    description: storageClassName overrides the default StorageClass.
                    type: string
                type: object
              strategy:
                default: Recreate
                description: |-
                  strategy overrides the default deployment update strategy.
                  Use "Recreate" when using RWO persistent volumes (default).
                enum:
                - Recreate
                - RollingUpdate
                type: string
              tls:
                description: tls configures TLS certificate provisioning.
                properties:
                  certManager:
                    description: certManager enables automatic TLS cert provisioning
                      via cert-manager.
                    type: boolean
                  issuerRef:
                    description: issuerRef references a custom cert-manager issuer.
                    properties:
                      kind:
                        description: kind is the issuer kind — Issuer or ClusterIssuer.
                        type: string
                      name:
                        description: name is the name of the issuer.
                        type: string
                    required:
                    - name
                    type: object
                  secretName:
                    description: |-
                      secretName references an existing TLS Secret (with tls.crt and tls.key).
                      If set, cert-manager is not used and the secret is mounted directly.
                    type: string
                type: object
              tolerations:
                description: tolerations for scheduling.
                items:
                  description: |-
                    The pod this Toleration is attached to tolerates any taint that matches
                    the triple <key,value,effect> using the matching operator <operator>.
                  properties:
                    effect:
                      description: |-
                        Effect indicates the taint effect to match. Empty means match all taint effects.
                        When specified, allowed values are NoSchedule, PreferNoSchedule and NoExecute.
                      type: string
                    key:
                      description: |-
                        Key is the taint key that the toleration applies to. Empty means match all taint keys.
                        If the key is empty, operator must be Exists; this combination means to match all values and all keys.
                      type: string
                    operator:
                      description: |-
                        Operator represents a key's relationship to the value.
                        Valid operators are Exists, Equal, Lt, and Gt. Defaults to Equal.
                        Exists is equivalent to wildcard for value, so that a pod can
                        tolerate all taints of a particular category.
                        Lt and Gt perform numeric comparisons (requires feature gate TaintTolerationComparisonOperators).
                      type: string
                    tolerationSeconds:
                      description: |-
                        TolerationSeconds represents the period of time the toleration (which must be
                        of effect NoExecute, otherwise this field is ignored) tolerates the taint. By default,
                        it is not set, which means tolerate the taint forever (do not evict). Zero and
                        negative values will be treated as 0 (evict immediately) by the system.
                      format: int64
                      type: integer
                    value:
                      description: |-
                        Value is the taint value the toleration matches to.
                        If the operator is Exists, the value should be empty, otherwise just a regular string.
                      type: string
                  type: object
                type: array
              version:
                description: version is the Warpgate image tag to deploy (e.g. "0.21.1",
                  "latest").
                type: string
            required:
            - adminPasswordSecretRef
            - version
            type: object
          status:
            description: status defines the observed state of WarpgateInstance
            properties:
              conditions:
                description: conditions represent the current state of the WarpgateInstance
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              connectionRef:
                description: connectionRef is the name of the auto-created WarpgateConnection
                  CR.
                type: string
              endpoint:
                description: endpoint is the internal service URL for the Warpgate
                  API.
                type: string
              readyReplicas:
                description: readyReplicas is the number of ready pods.
                format: int32
                type: integer
              version:
                description: version is the currently deployed version.
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      scale:
        specReplicasPath: .spec.replicas
        statusReplicasPath: .status.readyReplicas
      status: {}
